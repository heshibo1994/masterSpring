<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.mybatis.mapper.CustomerMapper">
<!--    查询用户-->
    <select id = "findUserById" parameterType="int" resultType="com.mybatis.mapper.User">
        select * from user where id = #{id}
    </select>
<!--    新增用户-->
    <insert id="insertUser" parameterType="com.mybatis.mapper.User">
        insert into user(username,birthday,sex,addr) value
        (#{username},#{birthday},#{sex},#{addr})
    </insert>
<!--    删除用户-->
    <delete id="deleteUserById" parameterType="int">
        delete from user where id=#{id}
    </delete>
<!--    修改用户-->
    <update id="updateUserSex" parameterType="com.mybatis.mapper.User">
        update user set sex = #{sex} where id =#{id}
    </update>
</mapper>


